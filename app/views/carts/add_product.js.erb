$(".header-ctn").html("<%= j render "shopping_cart" %>")
if (confirm('Add product to cart success! Are you sure order? Press no to continue buy')) {

} else {
  window.location.replace("<%= new_order_path %>")
}
